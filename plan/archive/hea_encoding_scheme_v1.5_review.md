# 高熵合金编码方案 v1.5 · 深度评审报告

**评审日期**：2026-06-03
**评审维度**：量子计算（QUBO/Ising模型 · 量子退火） + 高熵合金物理（第一性原理 · 热力学模型）
**方案文件**：`hea_encoding_scheme_v1.5.md`
**评审结论**：⚠️ 方案架构良好，但存在 **2 个根本性缺陷** 和 **5 个重要物理/工程问题**，需在进入编码阶段前修正。

---

## 一、总体评估

| 维度 | 评级 | 简评 |
|------|------|------|
| 编码方案设计 | ⭐⭐⭐⭐ | 分层精度编码（PrecisionSplit）思路正确，步长统一确保数学闭合 |
| QUBO映射正确性 | ⭐⭐⭐ | 天然二次型利用合理，但目标函数归一化缺失是致命缺陷 |
| 约束处理机制 | ⭐⭐⭐ | PenaltyFlex自适应框架完整，但 P0 单调递增策略有过度收缩风险 |
| 多目标优化理论 | ⭐⭐ | ParetoZoom 架构精巧，但 **加权和法无法恢复非凸前沿** 是数学硬伤 |
| 物理模型保真度 | ⭐⭐⭐ | 核心模型选择基本合理，但 C 元素的处理存在多处物理偏差 |
| 工程可行性 | ⭐⭐⭐⭐ | 38 变量在 550 量子比特硬件上资源充裕，双层自适应架构解耦清晰 |

---

## 二、根本性缺陷（Critical — 必须在编码前解决）

### 🔴 缺陷 1：目标函数量纲不统一，加权和失效

**问题定位**：§4.1 三目标设计 + §4.2 加权和聚合

三个目标函数的数据级差异巨大：

| 目标 | 符号 | 典型数值范围 | 量级 |
|------|------|-------------|------|
| f₁ | ΔH_mix | −20 ~ +5 kJ/mol | ~25 |
| f₂ | 密度 | 6.0 ~ 9.0 g/cm³ | ~3 |
| f₃ | 成本 | 任意单位（未定义） | 未知 |

**第一性原理分析**：

加权和 `H_obj = w₁·f₁ + w₂·f₂ + w₃·f₃` 中，即使 w₁ = w₂ = w₃ = 1/3（均匀权重），由于 f₁ 的数值范围是 f₂ 的约 8 倍，**实际有效权重比约为 8:1:?（取决于成本量纲）**。用户期望的 "形成能-密度各半" (w₁=w₂=0.5) 实际上变成了 "形成能占 89%，密度占 11%"。

这在数学上等价于权重被目标函数的 scale 隐性扭曲。量子退火器按 QUBO 系数的绝对值寻找 low-energy state，f₁ 的系数将主导 QUBO 矩阵的 spectral radius，密度和成本目标沦为扰动项。

**建议修正**：

```
方案 A（推荐）：Min-Max 归一化到统一区间
  f_i' = (f_i - f_i^min) / (f_i^max - f_i^min)
  其中 f_i^min, f_i^max 由粗网格 6 组求解后确定

方案 B：Z-score 标准化
  f_i' = (f_i - μ_i) / σ_i

方案 C：物理归一化
  f₁' = f₁ / 25  (kJ/mol)
  f₂' = f₂ / 9   (g/cm³)
  f₃' = f₃ / max_cost
```

> ⚡ **量子计算意义**：归一化还影响 QUBO 系数矩阵的条件数。系数跨量级会导致 Ising 模型能谱退化，降低量子退火隧道效应效率，等效于提高有效温度——退火器更难找到精确基态。

---

### 🔴 缺陷 2：加权和法数学上无法恢复非凸 Pareto 前沿

**问题定位**：§4.2.1 理论背景 + §4.2.3 ParetoZoom 算法

**第一性原理分析**：

加权和标量化 `min Σ w_i f_i(x)` 只能找到 Pareto 前沿的 **凸包（convex hull）** 上的点。对于非凸 Pareto 前沿的凹陷区域：

```
        f₂
        ↑
        |     A
        |      \
        |       \  ← 凹陷区域（非凸）
        |        B · · · C
        |              \
        |               D
        +──────────────────→ f₁
```

点 B 和 C 位于凹陷区域，**不存在任何非负权重 (w₁,w₂) 使得加权和最小值落在 B 或 C**。无论权重如何插值、高斯微扰，加权和法的解只会落在 A→D 的连线上（虚线），完全跳过 B 和 C。

**ParetoZoom 的 "间隙检测" 为什么无法修复**：

算法 §4.2.3 第 7-11 行检测相邻非支配解的距离 `||y_i − y_{i+1}||₂`。当检测到 A 和 D 之间距离过大时，插入中点权重并重新求解。但无论权重如何变化，加权和求解器返回的新解仍然会落在 A-D 连线（凸包）上——**不会落到凹陷区域的 B 或 C**。

这就像用直尺去量曲线的长度：直尺只能在两点之间画直线，永远量不到曲线的弯曲部分。

**模型层面的缓解因素**：

对于本方案的三个目标（ΔH_mix, 密度, 成本），它们之间存在强 trade-off（Al 含量升高→密度↓但 ΔH_mix↑），Pareto 前沿可能接近凸的。但 C 的引入带来非线性（碳化物形成、间隙固溶），可能产生非凸特征。**目前无法保证凸性**。

**建议修正**：

```
方案 A（推荐）：切换为 ε-约束法（ε-constraint method）
  主目标：min f₁（形成能）
  约束：f₂ ≤ ε₂（密度上限），f₃ ≤ ε₃（成本上限）
  扫描 ε₂, ε₃ 网格 → 可恢复完整 Pareto 前沿（含非凸区域）

方案 B：使用增强的标量化方法
  - 切比雪夫标量化（Tchebycheff）：min max{w_i|f_i - z_i*|}，可恢复非凸前沿
  - 增广 ε-约束法（AUGMECON）

方案 C：在方案文档中明确承认局限
  如果评估后认为三目标 Pareto 前沿近似凸（可通过粗网格结果验证），
  则保留加权和法，但需增加凸性验证步骤和风险评估。
```

> ⚡ **量子计算意义**：ε-约束法将多目标问题转化为一系列单目标约束优化问题——对 QUBO 框架天然友好。每个 ε 组合对应一次 PenaltyFlex + 量子求解，与当前框架完全兼容。采样量增加但质量提升。

---

## 三、重要物理问题（High — 影响优化的物理意义）

### 🟡 问题 1：C 的 ΔH_mix 处理存在物理错误

**问题定位**：§4.1 f₁ 形成能 + 九、参考文献

**第一性原理分析**：

Miedema 正规溶液模型 `ΔH_mix = 4 Σ_{i<j} ΔH_{ij}^{AB} c_i c_j` 适用于 **置换式固溶体**。C 作为间隙元素，其与过渡金属的相互作用涉及：
1. **间隙位占位**：C 进入八面体间隙，不占据置换位——正规溶液模型基于置换位随机混合假设
2. **强化合物形成倾向**：C-Cr 二元混合焓约 −30~−40 kJ/mol（对比置换元素间最大约 −22 kJ/mol 的 Al-Ni），远超固溶体范畴
3. **短程序/簇聚**：C 原子倾向于与 Cr、Fe 形成短程序甚至纳米碳化物前驱体，正规溶液的随机混合假设不成立

**后果**：QUBO 中的 ΔH_mix 项会将 C 含量推向高位（因为 C-Cr、C-Fe 的 ΔH 非常负），但这不代表稳定的固溶体——代表的是碳化物形成。优化器会被"欺骗"，得出高 C 的"最优解"。

**目前的缓解措施**：

文档 v1.5 已通过以下机制部分缓解：
- 后处理过滤器 §7：`c_C > 1.0%` 标记警告（但这是输出层，QUBO 内部仍可能偏向高 C）
- VEC 约束对 carbon 设置了`VEC_C = 4`近似值并标注风险
- §4.1 标注 "与相稳定性的关系" 提示避免与目标重复

**但这些不足以解决核心问题**。

**建议修正**：

```
方案 A：C 的 ΔH 贡献单独处理
  ΔH_mix_total = ΔH_mix^substitutional + ΔH_C^interstitial(c_C)
  其中 ΔH_C^interstitial 使用实验测定的偏摩尔溶解焓（~50-80 kJ/mol for C in FCC CoCrFeNi）
  而非正规溶液交叉项

方案 B：对 C 参与 ΔH_mix 交叉项施加折扣因子
  ΔH_{i,C}^{eff} = γ · ΔH_{i,C}^{Miedema}，γ ∈ [0.1, 0.3]
  反映间隙占位和化合物形成的非理想性

方案 C：f₁ 中移除 C 的贡献，C 的相稳定性由后处理+P1约束单独判断
```

---

### 🟡 问题 2：Vegard 定律不适用于间隙元素 C 的密度预测

**问题定位**：§4.1 f₂ 密度（Vegard 定律）

**第一性原理分析**：

Vegard 定律 `a = Σ c_i a_i` 描述置换式固溶体的晶格常数线性变化。对于密度：
```
ρ = (Σ c_i M_i) / (a³ · N_A / n_atoms_per_cell)
```

其中 M_i 为原子量，a 为晶格常数。Vegard 近似下 `a ∝ Σ c_i r_i`，且假设每个原子占据一个置换位。

**C 的特殊性**：
1. C 占据 **八面体间隙位**（在 FCC 中，单位晶胞有 4 个置换位和 4 个八面体间隙位）
2. 间隙 C 使晶格 **膨胀**（a 增大），而非收缩——与 ρ_C = 2.27 暗示的"C 使密度降低"方向可能 **相反**
3. 文献中 ρ_C = 2.27 g/cm³ 是石墨的密度，与间隙 C 原子的实际物理贡献毫无关系

**定量估算**：在 AlCoCrFeNi FCC 相中，添加 1 at% C 通常使晶格常数增加约 0.1-0.3%，但质量增加更显著。**净效果可能是密度轻微增加或基本不变**，而非 Vegard 线性混合预测的降低。

**建议修正**：

```
方案 A：使用第一性原理计算的偏摩尔体积
  ρ(c_C) = ρ_0 / (1 + β · c_C)
  其中 β 为 C 引起的体积膨胀系数（由 DFT 或实验确定，约 0.02-0.05/at% C）

方案 B：保守处理——f₂ 中忽略 C 的密度贡献
  仅用 Al, Co, Cr, Fe, Ni 的 Vegard 加权计算基础密度
  C 对密度的影响在后处理中标注

方案 C：使用实验数据校准
  引用含 C 的 AlCoCrFeNi 实验密度数据建立代理模型
```

---

### 🟡 问题 3：缺少 HEA 固溶体形成的关键判据参数

**问题定位**：§3.2.2 P1 相结构项（仅含 VEC）+ §7 后处理过滤器

**第一性原理分析**：

HEA 固溶体设计领域有三个标准判据，本方案仅使用了 VEC（价电子浓度），遗漏了另外两个：

| 判据 | 公式 | 固溶体形成阈值 | AlCoCrFeNi 等原子比 | 重要性 |
|------|------|-------------|-------------------|--------|
| **原子尺寸差 δ** | `δ = √(Σ c_i(1 − r_i/r̄)²)` | δ < 6.6% | ~5.2% | **高** — Al 原子较大，含量变化显著影响 δ |
| **Ω 参数** | `Ω = T_m·ΔS_mix/|ΔH_mix|` | Ω > 1.1 | ~2-5 | **高** — 综合热力学与动力学 |
| **VEC** | `VEC = Σ c_i·VEC_i` | 6.87-8.0 (双相) | ~7.2 | 已有 |

**为什么 Ω 参数重要**：

Ω > 1.1 意味着高熵效应（TΔS_mix）超过混合焓（|ΔH_mix|），固溶体稳定。本方案的 f₁ 目标最小化 ΔH_mix（使其更负），这与 Ω > 1.1 形成 **内在矛盾**：

- f₁ 想使 ΔH_mix 更负 → 减小 Ω → 可能低于 1.1 → 固溶体不稳定
- 这是本方案中 **最隐蔽也最关键的多目标冲突**：热力学稳定性（负 ΔH）vs 高熵固溶体形成能力（需要 |ΔH| 不大）

**为什么 δ 参数重要**：

Al 的原子半径（1.43 Å）显著大于 3d 过渡金属（1.25-1.28 Å）。当 Al 含量从 15% 升至 25%，δ 从约 4.5% 升至约 5.8%。虽然仍在 6.6% 阈值内，但接近边界。如果优化器为降低密度而推高 Al 含量（§4.1 f₂ 注释），可能无意中使 δ 超标。

**建议修正**：

```
方案 A（推荐）：将 δ 和 Ω 加入后处理过滤器
  IF δ > 6.6%:  标记 ⚠️ "原子尺寸差过大，固溶体不稳定风险"
  IF Ω < 1.1:   标记 ⚠️ "高熵效应不足以克服混合焓，可能形成金属间化合物"

方案 B：将 Ω 加入 P1 软约束（QUBO 内嵌）
  H_omega = (Ω − Ω_target)², Ω_target > 1.1
  注意：Ω 含 |ΔH_mix| 在分母，不是 c_i 的二次型——需要近似或代理模型

方案 C：将 δ 加入 P1 软约束
  δ² = Σ c_i(1−r_i/r̄)² 包含 r̄ = Σ c_j r_j 在分母，非二次型
  建议仅在后处理中使用，不在 QUBO 内部嵌入
```

---

### 🟡 问题 4：C 含量的编码范围远超物理溶解度

**问题定位**：§2.2 间隙元素 C 编码 + §1 体系特征分析

**第一性原理分析**：

| 相/合金 | C 溶解度 (at%) | 温度条件 | 来源 |
|---------|---------------|---------|------|
| FCC CoCrFeNi | <0.05 | RT | 实验测定 |
| FCC CoCrFeNi | ~0.3 | 1000°C | 热力学计算 |
| BCC AlCoCrFeNi | <0.1 | RT | 估计（BCC 间隙更小） |
| M₇C₃ / M₂₃C₆ 碳化物 | >1.0 | — | 化合物形成 |

本方案 C 编码范围 **[0, 1.75%]**，步长 0.25%。这意味着：
- 全部 8 个离散等级中，**至少 4 个（c_C ≥ 1.0%）在物理上属于碳化物形成区**
- c_C ≥ 0.25% 在实际固溶体中已属高碳（超出室温平衡溶解度 5-20 倍）
- 0% 和 0.25% 之外的所有等级几乎必然被后处理过滤器标记或在实际中形成碳化物

**量子资源浪费分析**：
- 3 个量子比特（C 编码）中，实际有用的物理区间可能只有 0% 和 0.25%
- 等效浪费率 ≈ 75%（3/4 的编码空间对应非固溶体状态）
- 更糟的是，这些"非物理"状态因 C-金属强负 ΔH（见问题 1），在 f₁ 目标中反而表现为"好解"

**建议修正**：

```
方案 A（推荐）：缩减 C 编码范围并匹配物理
  base_C = 0.0%, step_C = 0.05%, 3 bits → [0, 0.35%]
  或 step_C = 0.1%, 2 bits → [0, 0.3%]（节省 1 qubit）

方案 B：保持现有编码，但在 QUBO 中嵌入 C 上限软约束
  H_C_penalty = λ_C × max(0, c_C − 0.5%)²
  将超过 0.5% 的 C 含量作为惩罚，而非仅靠后处理过滤

方案 C：重新定义 C 的角色
  如果不追求 C 的固溶强化，可将 C 作为确定参数而非优化变量
  只在后处理中评估几个预设 C 水平（0, 0.1%, 0.25%, 0.5%）对 sol 的影响
  将释放的 3 个 qubit 用于提高主元精度
```

---

## 四、工程问题（Medium — 影响方案可靠性）

### 🟠 问题 5：PenaltyFlex 中 λ_sum 的单调递增策略可能导致过度惩罚

**问题定位**：§3.2.3 自适应更新规则 + 完整算法第 12 行

**分析**：

算法第 12 行规定 P0（成分和约束）的 λ 更新为：
```
λ_sum^(t+1) = MAX(λ_sum^(t), λ_sum^(t) × exp(α · tanh(v̄_sum)))
```

其中 `v̄_sum ≥ 0`（平均偏离度），因此 `exp(α · tanh(v̄_sum)) ≥ 1`，λ_sum **只增不减**。

问题场景：
1. 初期 v̄_sum 较大 → λ_sum 快速增长
2. λ_sum 达到足以强制约束满足的水平 → v̄_sum → 0
3. 此时 `exp(α · tanh(0)) = exp(0) = 1` → λ_sum 保持不变
4. **但 λ_sum 已经过大**：约束完美满足后，过大的 λ_sum 仍然压制目标的探索空间
5. 由于算法禁止 λ_sum 减小，这种过度惩罚无法自我纠正

**量化**：初始 λ_sum = 0.1。假设前 3 轮 v̄_sum ≈ 10%（成分和偏差大），α = 0.8：
- 轮 1: 0.1 → 0.1 × exp(0.8 × tanh(10)) ≈ 0.1 × 2.23 ≈ 0.22
- 轮 2: 0.22 → 0.22 × 2.23 ≈ 0.49
- 轮 3: 0.49 → 0.49 × 2.23 ≈ 1.10
- 轮 5: ~5.5，轮 7: ~27.5

λ_sum = 27.5 意味着成分和偏离 5% 的惩罚为 27.5 × 5² = **687.5**（任意单位）。此时 ΔH_mix 目标（范围 ~25）完全被惩罚淹没，QUBO 实质上退化为一台"约束求解器"而非"多目标优化器"。

**建议修正**：

```
方案 A（推荐）：改为对称自适应（允许减小）
  λ_sum^(t+1) = λ_sum^(t) × exp(α · tanh(v̄_sum − ε_sum))
  其中 ε_sum 为容忍偏差（如 1%）
  v̄_sum < ε_sum → λ_sum 减小，释放目标探索空间
  v̄_sum > ε_sum → λ_sum 增大，收紧约束

方案 B：添加上限
  λ_sum_max = λ_obj_avg × 10  // 惩罚项不超过目标项的 10 倍
  λ_sum^(t+1) = MIN(λ_sum_max, MAX(...))

方案 C：采用 PF²（Parameter-Free Penalty Function）策略
  从解的可行性启发式估计所需 λ 界限，而非迭代增长
```

---

### 🟠 问题 6：PenaltyFlex 收敛判据过于宽松

**问题定位**：§3.2.3 完整算法第 14 行

**分析**：

收敛条件 `MAX_j |λ_j⁽ᵗ⁺¹⁾ − λ_j⁽ᵗ⁾| / λ_j⁽ᵗ⁾ < γ` 中 γ = 0.5。

这意味着 λ 的 **相对变化小于 50%** 即判定收敛。这非常宽松：

- 如果 λ_sum = 10.0，变化 ±4.99（→ 5.01 或 14.99）仍算"收敛"
- λ 变化 50% 会导致惩罚项变化 50%，足以改变 QUBO 解的性质
- 标准的相对容忍度通常取 0.01-0.05（1%-5%），而非 0.5

**建议修正**：

```
γ 改为 0.05（5%）或 0.10（10%），并增加最大迭代数 T_max 的安全网。
```

---

### 🟠 问题 7：缺少 C 与过渡金属的 ΔH_ij^AB 值表

**问题定位**：§4.1 f₁ 形成能

**分析**：

文档详细给出了主元编码、密度参数等，但未给出 ΔH_mix 计算所需的 **二元混合焓参数表**。这些参数是 QUBO 矩阵组装的必需输入。

需要明确的值（Miedema 模型，kJ/mol）：

|  | Al | Co | Cr | Fe | Ni | C |
|--|-----|-----|-----|-----|-----|----|
| **Al** | 0 | −19 | −10 | −11 | −22 | **?** |
| **Co** | — | 0 | −4 | −1 | 0 | **?** |
| **Cr** | — | — | 0 | −1 | −7 | **?** |
| **Fe** | — | — | — | 0 | −2 | **?** |
| **Ni** | — | — | — | — | 0 | **?** |
| **C** | — | — | — | — | — | 0 |

其中 C-X 对的 ΔH 值（标记 **?**）是本方案最关键但缺失的参数。这些值的符号和大小直接决定 QUBO 是否会将 C 含量推向非物理高位（见问题 1）。

**建议修正**：

```
在方案中补充完整的 ΔH_ij^AB 参数表，标注数据来源和不确定性。
对 C-X 对，既要给出 Miedema 模型值，也要标注其物理局限性。
```

---

### 🟠 问题 8：参考点 r 的静态定义可能导致 HV 计算失真

**问题定位**：§4.2.3 算法输入 `参考点 r = (max f₁, max f₂, max f₃)`

**分析**：

初始 6 组粗网格解可能只覆盖目标空间的一小部分。以粗网格解的 max f_i 作为参考点：
- 如果后续 ParetoZoom 发现扩展前沿的解（比粗网格 max 更大），这些解会落在参考点之外 → HV 为负或未定义
- 需要动态更新参考点，或使用安全裕量（如 1.1 × max）

**建议修正**：

```
r_i = 1.2 × max(f_i across P₀)  // 20% 安全裕量
并在每轮 ParetoZoom 迭代后检查是否需要更新 r。
```

---

## 五、次要问题与改进建议（Low）

### ⚪ 问题 9：PenaltyFlex 算法的步数标注不一致

文档标题写 "完整PenaltyFlex算法（8步）"，但算法内部使用了 1-16 的行号标注。实际逻辑步骤数大约为 6-7 步（与标注的 8 步接近），但行号带来的混淆应消除。

**建议**：将行号改为步骤编号，或移除 "8步" 的声明。

### ⚪ 问题 10：VEC 约束与 f₁ 目标的数值竞争

§3.2.2 中 VEC 目标区间为 7.0-7.6（中心 7.3），约束 H_VEC = (VEC − 7.3)²。但优化器可能通过调整成分同时降低 ΔH_mix（f₁）和接近 VEC = 7.3。两者之间的关系未分析：
- Al 含量 ↑ → VEC ↓（Al VEC=3）且 ΔH_mix ↓（Al 与 TM 的 ΔH 负）
- 这意味着 f₁ 和 VEC 约束可能 **同向拉动**，而非互相制约
- 真正的 trade-off 在密度和成本之间

**建议**：分析三目标 + VEC 约束之间的 Jacobian 关系，标注哪些是真正竞争的。

### ⚪ 问题 11：主元含量范围 [5%, 36.75%] 与实际推荐范围 [15%, 25%] 不匹配

§1 表格给出 "典型含量范围 15~25 at%"，但 §2.1 编码将下限设为 5%。这意味着约 40% 的编码区间（k_i ∈ [0, 40)，对应 5%~15%）位于"非典型"区域。这些区域可能产生无物理意义的解，浪费量子资源。

**建议**：考虑将 base_main 调整为 10% 或添加 QUBO 内部的成分范围软约束。

### ⚪ 问题 12：参考文献质量

| 编号 | 文献 | 问题 |
|------|------|------|
| [3] | Kurebayashi et al., IEICE, 2025 | 缺少具体期刊/会议名和卷号页码 |
| [7] | Lu et al., arXiv:2509.11452, 2025 | 预印本，未同行评议 |
| [8] | Emergent Mind, 2026 | 非学术来源（可能是 AI 生成的综述网站），不应作为学术引用 |

**建议**：[7] 标注为 preprint；[8] 替换为正式学术文献或删除；[3] 补全引用信息。增加碳在高熵合金中的关键实验文献。

---

## 六、方案亮点（值得保留的设计）

以下设计选择是正确的，应在后续版本中保留：

1. **分层精度编码（PrecisionSplit）**：用不同位数处理跨量级元素，数学上 elegant 且资源高效
2. **统一步长 0.25%**：使成分和约束可精确满足（K=300 整数解存在），避免了浮点精度导致的约束违反
3. **双层自适应架构**：ParetoZoom（外层权重自适应）+ PenaltyFlex（内层 λ 自适应）的解耦设计清晰，职责分明
4. **Warm-start 缓存**：相邻权重复用 λ 终值，减少重复收敛，工程上实用且正确
5. **后处理物理过滤器**：不修改 QUBO 结构，不消耗量子资源，作为输出层清洗——设计模式正确
6. **P1/P2 分层约束优先级**：硬约束（成分和）→ 强偏好（相结构）→ 弱偏好（成本），层次清晰
7. **VEC 风险标注**：对 C 的 VEC=4 近似主动标注风险，显示了良好的物理审慎性
8. **硬件兼容性验证**：38 变量 vs 550 qubit 的余量分析，14.5× 变量裕度和 214× 耦合裕度

---

## 七、修正优先级路线图

```
高优先级（进入编码前必须解决）：
┌─────────────────────────────────────────────────────┐
│ 1. 目标函数归一化（缺陷 1）                            │
│    → 在 §4.1 增加 Min-Max 归一化层，粗网格后自动校准    │
│                                                       │
│ 2. Pareto 前沿恢复策略（缺陷 2）                        │
│    → 评估前沿凸性，必要时切换 ε-约束法或切比雪夫标量化   │
│                                                       │
│ 3. C 的 ΔH_mix 物理修正（问题 1）                       │
│    → 间隙 C 的混合焓单独处理，不混入正规溶液模型         │
└─────────────────────────────────────────────────────┘

中优先级（影响优化质量）：
┌─────────────────────────────────────────────────────┐
│ 4. C 密度模型修正（问题 2）                             │
│ 5. 补充 δ 和 Ω 判据（问题 3）                           │
│ 6. C 编码范围重新校准（问题 4）                          │
│ 7. λ_sum 改为对称自适应（问题 5）                        │
│ 8. 收敛判据收紧（问题 6）                                │
└─────────────────────────────────────────────────────┘

低优先级（改进完整性和清晰度）：
┌─────────────────────────────────────────────────────┐
│ 9. 补充 ΔH_ij 参数表（问题 7）                           │
│ 10. 参考点动态更新（问题 8）                              │
│ 11-14. 其他次要问题                                    │
└─────────────────────────────────────────────────────┘
```

---

## 八、评审总结

方案 v1.5 展示了对 HEA-QUBO 交叉问题的深刻理解，编码设计和双层自适应架构体现了成熟的系统工程思维。三个目标函数均能天然映射到 QUBO 二次型，这是方案的核心技术优势。

但存在 **两个根本性缺陷**：
1. 目标函数未归一化——导致用户指定的权重在数学上被量级差异扭曲，实际优化方向偏离预期
2. 加权和标量化无法恢复非凸 Pareto 前沿——ParetoZoom 的间隙检测和微扰机制在数学上无法修复此问题

以及 **一个物理准确性问题集群**：C 作为间隙元素，在 ΔH_mix（正规溶液）、密度（Vegard 定律）、VEC（价电子浓度）三个模型中的处理均存在不同程度的物理偏差，可能系统性误导 QUBO 的优化方向。

**总体判断**：方案架构优秀，但在 **目标归一化** 和 **多目标标量化方法选择** 两个关键决策点需要修正。物理模型方面，C 的处理需要从 "近似" 升级到 "物理合理"。建议在修正上述问题后再进入 kaiwu SDK 编码阶段。

---

*评审人：AI 量子计算 + 高熵合金交叉领域分析*
*方法论：第一性原理推导 + 数学严格性验证 + 物理模型保真度审查 + 工程可行性评估*
